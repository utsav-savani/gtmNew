import 'package:flutter/material.dart';
import 'package:gtm/_shared/constants/string_constants.dart';

class WUserManagementPage extends StatelessWidget {
  const WUserManagementPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text(userManagement));
  }
}
