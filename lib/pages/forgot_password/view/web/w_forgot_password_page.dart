import 'package:flutter/material.dart';

class WForgotPasswordPage extends StatelessWidget {
  const WForgotPasswordPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
